name: Scheduled Order Report

on:
  schedule:
    # Runs every 5 minutes
    - cron: '*/5 * * * *'
  workflow_dispatch: # Allows manual triggering

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  send-email:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'
          
      - name: Install dependencies
        run: |
          echo "📦 Installing dependencies..."
          npm ci --prefer-offline 
          
      - name: Verify Resend installation
        run: |
          echo "🔍 Verifying resend installation..."
          node -e "console.log('resend version:', require('resend/package.json').version)"
        
      - name: Send order report email
        env:
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          RESEND_FROM_EMAIL: ${{ secrets.RESEND_FROM_EMAIL }}
          ORDER_REPORT_TO_EMAIL: ${{ secrets.ORDER_REPORT_TO_EMAIL }}
        run: |
          echo "🚀 Starting email send process..."
          node .github/scripts/send-order-report.js
          
      - name: Report status
        if: always()
        run: |
          if [ $? -eq 0 ]; then
            echo "✅ Order report email sent successfully!"
          else
            echo "❌ Order report email sending failed!"
            exit 1
          fi